<DocumentReaderScreen>:
    pos_hint: {"center_x": 0.5, "center_y": 0.5}

    ActionBar:
        id: doc_action_bar
        pos_hint: {"center_x": 0.5, 'top': 1}
        size_hint: None, None
        height: root.ACTION_BAR_HEIGHT
        width: doc_page.width

        ActionView:
            use_separator: False

            ActionPrevious:
                title: root.action_bar_title
                color: root.ACTION_BAR_TITLE_COLOR
                markup: True
                with_previous: False
                app_icon: root.app_icon_filepath

            ActionOverflow:

            BarButton:
                id: close_button
                text: "Close"
                icon: str(sys_paths.get_barks_reader_close_icon_file())
                important: True
                on_press: root.close()

    BoxLayout:
        id: doc_page
        orientation: "vertical"
        pos_hint: {"center_x": 0.5, "center_y": 0.5}
        size_hint: None,None
        height: root.height
        width: (self.height - doc_action_bar.height) / root.ASPECT_RATIO
        # Add padding to the top to make space for the ActionBar.
        padding: [dp(0), doc_action_bar.height, dp(0), dp(0)]

        Image:
            id: page_image
            color: 1,1,1,1
            opacity: 1
            source: root.page_source
            fit_mode: "contain"
            mipmap: True
