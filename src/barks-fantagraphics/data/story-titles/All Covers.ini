[info]
title = All Covers
source_comic = FANTA_02

[pages]
# The body pages of this collection are not listed here. They are derived from
# COVER_LOCATIONS (see barks_covers.get_cover_collection_pages) so the table
# stays the single source of truth. The pages are pre-baked offline as
# sequential "extra" pages in this volume's (FANTA_02) override + segments
# (guarded by tests/test_build_covers_collection.py).
