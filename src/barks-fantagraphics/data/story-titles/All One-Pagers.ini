[info]
title = All One-Pagers
source_comic = FANTA_01

[pages]
# The body pages of this collection are not listed here. They are gathered across
# volumes from ONE_PAGER_LOCATIONS (see comic_book_info.get_one_pager_collection_pages)
# so the table stays the single source of truth. source_comic above is only a nominal
# anchor volume; each page carries its own source volume.
