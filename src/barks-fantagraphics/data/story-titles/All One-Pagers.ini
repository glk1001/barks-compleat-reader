[info]
title = All One-Pagers
source_comic = FANTA_01

[pages]
# The body pages of this collection are not listed here. They are derived from
# ONE_PAGER_LOCATIONS (see comic_book_info.get_one_pager_collection_pages) so the
# table stays the single source of truth. The pages are pre-baked offline as
# sequential "extra" pages in this volume's (FANTA_01) override + segments
# (guarded by tests/test_build_one_pagers_collection.py).
