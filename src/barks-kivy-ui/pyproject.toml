[project]
name = "barks-kivy-ui"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "kivy==2.3.1",
    "pillow",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/barks_kivy_ui"]
