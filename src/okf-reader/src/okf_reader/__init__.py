"""Standalone reader for Open Knowledge Format (OKF) bundles.

A self-contained workspace package: `core` holds the Kivy-free parse/render/model
layer, `ui` holds the Kivy viewer. It has no dependency on the Barks comic domain,
so it can be run on its own (see scripts/read_okf.py) or embedded by another app.
"""
